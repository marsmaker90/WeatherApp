package com.poc.weatherapp

class BaseConfig {
    companion object {
        const val FORMAT_DATE = "DD-MMM-YYYY"
        const val FORMAT_TIME = "hh:mm:ss"
        const val FORMAT_HOURS = "ha"
    }
}